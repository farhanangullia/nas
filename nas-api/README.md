# API

This directory maintains the business logic for the microservice APIs written in Go.

## Local development

Install env tools:
`make install-tools`

Lint:
`make lint`

## Run locally

`make run-local`